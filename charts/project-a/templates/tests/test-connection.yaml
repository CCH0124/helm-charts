apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "project-a.fullname" . }}-test-connection"
  labels:
    {{- include "project-a.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "project-a.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
